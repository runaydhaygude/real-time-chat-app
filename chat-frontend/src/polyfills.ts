// polyfills.ts

/** 
 * Web Animations `@angular/platform-browser/animations` polyfill.
 * 
 * In certain scenarios (e.g., Angular Universal), you need to include 
 * the polyfill for the Web Animations API.
 * 
 * You can comment this out if you are not using animations.
 */
// import 'web-animations-js';  // Run `npm install --save web-animations-js`.
/** 
 * Zone JS is required by Angular itself.
 */
import 'zone.js';  // Included with Angular CLI.

/**
 * Global variable shim
 */
(window as any).global = window; // Shim for global variable

// Other polyfills can be added here...
