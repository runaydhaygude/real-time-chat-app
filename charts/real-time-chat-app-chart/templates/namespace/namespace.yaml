# apiVersion: v1
# kind: Namespace
# metadata:
#   name: {{ .Values.namespace }}
#   annotations:
#     helm.sh/hook: pre-install  # This ensures it runs before the rest of the resources are applied.
